---
name: vite-react-patterns
description: Guides Vite + React SPA development including features, hooks, and HMR workflow. Use when working in Vite React projects detected by Agent Kit.
---

# Vite React Patterns

1. Read existing router and folder layout before adding routes.
2. Use `src/features/` for page-level modules when present.
3. Verify with `{{devCommand}}` after changes.
