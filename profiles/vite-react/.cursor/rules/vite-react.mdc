---
description: Vite + React SPA conventions
globs: src/**/*
alwaysApply: false
---

# Vite + React

- Prefer functional components with strict TypeScript.
- Colocate feature modules under `src/features/` when the project uses that layout.
- Use the project's chosen router (TanStack Router, React Router) — follow existing patterns.
- Dev server: `{{devCommand}}`
